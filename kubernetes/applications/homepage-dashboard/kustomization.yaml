apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: homepage-dashboard

configMapGenerator:
  - name: homepage
    files:
      - config.yaml

resources:
  - namespace.yaml
  - serviceaccount.yaml
  - service.yaml
  - rbac.yaml
  - deployment.yaml
  - ingress.yaml
