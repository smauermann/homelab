podDisruptionBudget:
  minAvailable: 0
  # maxUnavailable: 1

deployment:
  envoyGateway:
    resources:
      limits:
        memory: 1024Mi
      requests:
        cpu: 100m
        memory: 256Mi
  replicas: 2
