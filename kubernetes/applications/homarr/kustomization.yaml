apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: homarr

resources:
  - namespace.yaml

helmCharts:
  - name: homarr
    repo: https://oben01.github.io/charts/
    version: 1.2.6
    releaseName: homarr
    namespace: homarr
    valuesFile: values.yaml
