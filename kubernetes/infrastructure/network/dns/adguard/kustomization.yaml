apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

configMapGenerator:
  - name: adguard-config
    namespace: dns
    files:
      - AdGuardHome.yaml

generatorOptions:
  disableNameSuffixHash: true

resources:
  - users.yaml
  - service.yaml
  - rollout.yaml
  - ingress.yaml
