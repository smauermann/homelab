apiVersion: v1
kind: Service
metadata:
  name: unbound
  namespace: dns
spec:
  type: ClusterIP
  ports:
    - name: dns-tcp
      port: 53
      protocol: TCP
    - name: dns-udp
      port: 53
      protocol: UDP
  selector:
    app.kubernetes.io/name: unbound
