apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: servarr-media-nfs
spec:
  storageClassName: nfs-csi-retain
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Ti
